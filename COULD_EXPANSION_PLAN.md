# Could 확장 실행 계획

> 문서 상태: C1·C2·C3 완료 · C4 추가 캐릭터 외형 슬롯 착수 대기
> 최근 갱신: 2026-09-07

## 0. 재개 규칙

1. `ROADMAP.md`와 이 문서의 진행판을 확인한다.
2. C1~C4 중 첫 미완료 단계의 첫 미완료 체크박스부터 작업한다.
3. 각 단계는 규칙 잠금 → 회색 상자/기술 구현 → 자동 검사 → 브라우저 확인 → 사용자 승인 → 커밋 순서로 닫는다.
4. 새 이미지가 필요한 단계는 배치·코스 승인 뒤 흑백 앵커를 먼저 승인받고, 최종 에셋은 앵커 승인 뒤 한 번 생성한다.
5. 사용자 승인 전에는 다음 Could 단계에 착수하지 않는다.
6. 어린이·키보드·게임패드 플레이테스트 미실시 항목은 자동 검사나 브라우저 고정 상태 검수로 대체 완료 처리하지 않는다.

## 1. 진행판

| 단계 | 범위 | 상태 | 승인 문구 |
|---|---|---|---|
| C1 | 추가 짧은 챌린지 레벨 | 승인 완료 — 2026-09-04 | `C1 최종 승인` |
| C2 | 결과 화면 업적 스티커 | 승인 완료 — 2026-09-07 | `C2 최종 승인` |
| C3 | 로컬 2P 프로토타입 | 승인 완료 — 2026-09-07 | `C3 최종 승인` |
| C4 | 추가 캐릭터 외형 슬롯 | 착수 대기 | 미정 |

## 2. C1 — 추가 짧은 챌린지 레벨

### 목표와 잠금 전 규칙

- 정규 스테이지 5개를 모두 통과한 뒤 열리는 `level-06`을 추가한다. 이름은 **무지개 이어달리기**, 진행 방향은 오른쪽이다.
- 새 핵심 기믹·보스·비밀 공간 없이 기존 이동, 변신, 움직이는 발판, 붕괴 발판, 호박 장애물과 기존 적을 짧게 재조합한다.
- 월드 폭은 5,120px, 목표 플레이 시간은 일반 150~210초, 쉬운 모드 180~240초로 제한한다.
- 필수 목표는 `reach_gate` 하나다. 변신과 상단 지름길은 보상 경로이며 기본 캐릭터 상태만으로 지상 안전 경로를 완주할 수 있어야 한다.
- 신규 배경·타일셋·BGM·SFX는 만들지 않고 `level-01`의 밝은 들판 계열 에셋을 재사용한다. 코스 승인 후 선택 카드용 전용 미리보기만 별도 앵커 절차를 거친다.
- 피격 직후 연속 피격, 화면 밖 착지, 신뢰 점프를 만들지 않는다. 위험물은 최소 320px 앞에서 보이고 각 복합 시험 앞에는 안전 발판을 둔다.
- 체크포인트 부활 위치에는 적·호박·낭떠러지·움직이는 발판이 겹치지 않으며, 마지막 체크포인트는 체력과 비행 에너지를 회복한다.

### 코스 계약

| 구간 | 범위 | 학습/시험 | 안전 경로와 보상 경로 |
|---|---:|---|---|
| `relay_start` | 0~768 | 조작 재확인과 첫 별 아크 | 평지와 낮은 단차만 사용 |
| `relay_unicorn` | 768~1,920 | 호박 간격과 유니콘 돌파 | 지상 우회 가능, 돌파 시 상단 별 보상 |
| `relay_flight` | 1,920~3,328 | 움직이는 발판·짧은 낭떠러지·날개 선택 | 고정/이동 발판으로 완주 가능, 비행은 별 지름길 |
| `relay_mix` | 3,328~4,608 | 붕괴 발판과 예고형 적의 한 번짜리 복합 시험 | 시작 안전대와 중간 체크포인트 제공 |
| `relay_finish` | 4,608~5,120 | 회복·결과 정리·무지개 게이트 | 위험물과 적 없음 |

### 데이터 잠금값

- 레벨: `id: level-06`, `order: 6`, `schemaVersion: 1`, `world: 5120×768`, `tileSize: 64`.
- 스폰: `(128, 576)`, 출구: `(4944, 576)`, `enterFrom: right`.
- 체크포인트: `(1792, 576)`, `(3264, 576)`, `(4544, 576)`이며 마지막만 `restoresHealth: true`.
- 쉬운 모드는 `(2688, 576)` 체크포인트를 하나 더 제공하고, 적 1종 제거, 이동 발판 속도 0.75배, 붕괴 지연 1.5배, 비행 소모 0.65배, 낭떠러지 점수 손실 0을 적용한다.
- 선택 목표: 별 30개, 180초 이내, 무피해. 점수 보상은 각각 500/400/800점이다.
- 코스 전체 적은 최대 4개, 호박은 최대 5개, 낭떠러지는 2개 이하, 움직이는 발판은 2개 이하, 붕괴 발판은 2개 이하로 제한한다.
- 카메라 단서는 첫 호박, 첫 낭떠러지, 복합 시험에만 두며 기존 1280×720 화면에서 다음 안전 착지점을 함께 보여준다.

### 세부 개발 단계

#### C1.1 규칙·코스 설계

- [x] 기존 5개 레벨의 길이, 기믹, 목표와 게이트 완료 흐름 대조
- [x] 짧은 플레이 시간, 재사용 범위, 필수/선택 경로와 난이도 상한 문서화
- [x] 사용자 규칙 승인 — 2026-09-04 사용자 `C1 규칙 승인`
- [x] Tiled 회색 상자용 지형 좌표, 적·아이템·기믹·카메라 단서 좌표표 작성
- [x] 1280×720 구간별 정지 화면으로 선행 시야와 착지 안전성 검수
- [x] 사용자 코스 승인 — 2026-09-04 사용자 `C1 코스 승인`

#### C1.2 회색 상자 구현

- [x] `assets/levels/level-06/tilemap.json`과 `src/data/levels/level-06.js` 추가
- [x] 레벨 레지스트리, HMR 목록, 순차 해금과 다음 스테이지 흐름에 `level-06` 연결
- [x] 기존 배경·타일·오브젝트·오디오 키만 사용해 회색 상자 로딩
- [x] 기본 캐릭터 안전 경로, 세 변신 보상 경로, 체크포인트·게이트 완료 구현
- [x] 기존 `visualReview` 구간 고정 진입점과 디버그 상태에서 `level-06` 확인

#### C1.3 자동 검증

- [x] 스키마, 섹션 연속성, 월드 범위, ID 중복, 참조 에셋과 tilemap 검증
- [x] 보스 없음, `reach_gate` 필수, 별 30개 이상, 위험/기믹 상한과 안전 여백 검증
- [x] `getNextLevel(level-05)`, 6번 카드 등록, 최종 레벨 다음 단계 없음 검사
- [x] 일반/쉬운 모드 데이터 불변성과 난이도 보정 검사
- [x] `npm run test`, `npm run validate`, `npm run build`, `git diff --check` 통과

#### C1.4 브라우저·입력 검수

- [ ] 일반 모드에서 기본 상태 안전 경로와 세 변신 지름길 확인
- [ ] 쉬운 모드 추가 체크포인트·기믹 완화·점수 손실 0 확인
- [ ] `fallback=1`, `effects=reduced`, 음소거에서 실제 완주와 진행 단서 유지 확인
- [ ] 6번 선택 카드, 잠금/해금, 클리어 뒤 결과 화면과 스테이지 선택 복귀 확인
- [x] 구간 고정 화면과 쉬운 모드 `fallback=1&effects=reduced&mute=1` 진입에서 콘솔 warning/error 0건 확인
- [ ] 키보드 실제 완주 확인
- [ ] 게임패드 실제 완주 확인

#### C1.5 선택 카드 미리보기

- [x] 코스 승인 화면을 바탕으로 문자 없는 흑백 16:9 앵커 제작
- [x] 25% 축소에서 주 경로, 움직이는 발판과 무지개 게이트 판독 확인
- [x] 사용자 미리보기 앵커 승인 — 2026-09-04 사용자 `다음 단계 진행`
- [x] 승인 앵커 기반 최종 미리보기 1회 생성, manifest·mapping 연결
- [x] 선택 카드의 잠금/검수용 해금/선택 상태와 fallback 검수

#### C1.6 종료 게이트

- [x] 검수 주소, 재현 절차, 화면과 미실시 플레이테스트를 최종 리뷰 문서에 기록
- [x] 사용자 최종 승인 — 2026-09-04 사용자 `C1 최종 승인`
- [x] 승인 범위만 `feat: add rainbow relay challenge level`로 커밋
- [x] `ROADMAP.md`와 이 문서 진행판을 완료 상태로 갱신

코스 좌표, 검수 주소와 1280×720 화면은 `references/COULD1_CHALLENGE_COURSE_REVIEW.md`를 따른다.

## 3. C2 — 결과 화면 업적 스티커

### 확정 범위

**결과 화면 업적 스티커**로 확정했다. 사진 모드는 이번 C2에서 제외한다. 기존 `ClearScene`의 선택 목표 결과 카드가 달성 내용을 글로 설명하고, 새 스티커는 승리 캐릭터 주변을 꾸미는 문자 없는 시각 보상만 담당한다.

### 사진 모드와 비교

| 항목 | 사진 모드 | 결과 화면 스티커 — 권장 |
|---|---|---|
| 변경 범위 | `GameScene`, `UIScene`, 입력·일시정지·캔버스 캡처·다운로드 | `ClearScene`, 순수 스티커 데이터, UI 에셋 |
| 저장/개인정보 | 로컬 이미지 다운로드와 브라우저 권한·실패 처리 필요 | 파일·개인정보·영구 저장 없음 |
| 기존 기능 활용 | 새 캡처 흐름 필요 | 기존 `achieved`, `clearReview`, 목표 카드·UI SFX 재사용 |
| 접근성 위험 | HUD 숨김과 캡처 상태를 별도 안내해야 함 | 결과 요약에 스티커 이름만 추가 |
| 회귀 위험 | 플레이 중 정지·복귀·모바일/브라우저 차이 | 결과 화면 안에서만 제한 |

### 승인된 규칙

- 스티커는 결과 화면에서만 보이고 게임 플레이·점수·목표 판정·해금에 영향을 주지 않는다.
- `무지개 클리어` 스티커는 항상 한 개 표시한다. 달성한 선택 목표에 따라 `별 수집가`, `비밀 탐험가`, `번개 질주`, `완벽한 모험`을 각각 최대 한 개 추가한다.
- 기존 레벨의 알려진 선택 목표 네 종류만 허용한다. 알 수 없는 값과 중복 값은 무시하며 총 스티커 수는 최대 5개다.
- 배치는 캐릭터 초상 주변의 고정 슬롯 5곳을 사용한다. 이름·기록·목표 카드·버튼과 겹치지 않으며 캐릭터 얼굴과 몸통을 가리지 않는다.
- 별도 선택·드래그·신규 키 조작은 만들지 않는다. 결과 진입 시 결정적 순서로 자동 배치해 어린이와 키보드·게임패드 사용자가 다음 단계 조작을 그대로 유지한다.
- 스티커 배치는 세션 한정이다. `ProgressManager` 스키마와 저장 데이터는 바꾸지 않고 이미지 다운로드·클립보드·네트워크 전송을 하지 않는다.
- 일반 효과는 짧은 1회 도장 팝만 허용한다. `effects=reduced`에서는 이동·회전·스케일 튕김 없이 100ms 이내 페이드만 사용한다.
- 정상 에셋이 없거나 `fallback=1`이면 동일 슬롯에 코드 도형 실루엣을 표시한다. 문자·이모지 폰트에 의존하지 않는다.
- 접근성 상태는 스테이지·캐릭터·기록·점수·달성 목표에 이어 표시된 스티커 이름을 읽는다. 장식은 필수 정보의 유일한 전달 수단이 아니다.
- 새 BGM·SFX는 만들지 않고 기존 `sfx_ui_select`를 낮은 볼륨으로 최대 3회만 재사용한다.

### 예상 파일

- 신규: `src/data/resultStickers.js`, `scripts/build-c2-result-stickers.js`
- 수정: `src/scenes/ClearScene.js`, `src/scenes/BootScene.js`, `src/systems/AssetManager.js`
- 수정: `assets/manifest.json`, `references/mapping.json`, `ASSET_LIST.md`, `ASSET_PROMPTS.md`
- 검증: `scripts/test-core.js`, `scripts/validate-structure.js`, `scripts/validate-assets.js`, `TEST_CHECKLIST.md`
- 검수: `references/COULD2_STICKER_PLACEMENT_REVIEW.md`, `references/COULD2_STICKER_ANCHOR_REVIEW.md`, `references/COULD2_STICKER_FINAL_REVIEW.md`

### 세부 개발 단계

#### C2.1 범위·규칙 잠금

- [x] 사진 모드와 결과 화면 스티커의 변경 범위·저장·접근성·회귀 위험 비교
- [x] 결과 화면 스티커 권장안, 다섯 종류, 자동 배치, 무저장·무다운로드 규칙 문서화
- [x] 사용자 범위·규칙 승인 — 2026-09-07 `C2 규칙 승인`

#### C2.2 데이터·회색상자

- [x] `resultStickers.js`에 허용 타입, 표시명, 우선순위, 5개 안전 슬롯과 최대 수 정의
- [x] `achieved` 정규화, 중복·알 수 없는 값 제거, 항상 표시되는 클리어 스티커를 순수 함수로 구현
- [x] `ClearScene`에 코드 도형 스티커와 1회 등장 연출 연결
- [x] `clearReview`에 0·1·3·4개 목표 조합을 고정 재현하는 주소 마련
- [x] 1280×720, `effects=reduced`, `fallback=1`에서 겹침과 판독성 검수
- [x] 사용자 배치 승인 — 2026-09-07 `C2 배치 승인`

#### C2.3 자동 검증

- [x] 목표 없음/중복/알 수 없음/4종 전부 입력의 결정성·최대 5개·불변성 검사
- [x] 슬롯 중복, 화면 경계, 초상·텍스트·카드·버튼 금지 영역 충돌 검사
- [x] 점수·진행 저장·다음 스테이지·Esc 복귀 결과가 C2 전후 동일함을 구조 검사
- [x] 일반 효과 SFX 상한과 reduced 효과의 무이동·무회전 계약 검사
- [x] `npm run test`, `npm run validate`, `npm run build`, `git diff --check` 통과

#### C2.4 스티커 아트

- [x] 승인 회색상자를 바탕으로 문자 없는 5종 흑백 접촉 시트 앵커 1장 제작
- [x] 25% 축소와 이진 실루엣에서 무지개·별·열쇠구멍·날개 시계·하트 방패 구분
- [x] 사용자 앵커 승인 — 2026-09-07 `C2 앵커 승인`
- [x] 승인 앵커 기반 최종 컬러 시트 1회 생성 후 128×128 PNG 5종으로 분리
- [x] manifest·mapping·`AssetManager` 연결과 정상/fallback 비교

#### C2.5 브라우저·접근성 검수

- [x] 0·1·3·4개 목표 조합에서 최대 5개 스티커와 목표 카드가 함께 판독됨
- [x] 두 캐릭터, 여섯 레벨, 마지막 레벨의 단일 복귀 버튼 배치를 표본 검수
- [x] 포인터로 다음 스테이지와 스테이지 선택 복귀 경로가 결과 화면을 벗어남
- [ ] 실제 키보드 Space/Z 다음 단계와 Esc 복귀, 실제 게임패드 복귀 확인
- [x] `effects=reduced`, 음소거, `fallback=1`에서 정보와 조작이 유지됨
- [x] 접근성 상태가 스티커 이름을 추가로 읽되 기존 결과 요약을 보존함
- [x] 콘솔 warning/error 0건 확인

#### C2.6 종료 게이트

- [x] 검수 주소, 화면, 자동 검사, 미실시 실제 입력 테스트를 최종 리뷰에 기록
- [x] 사용자 최종 승인 — 2026-09-07 사용자 `C2 최종 승인`
- [x] 승인 범위만 `feat: add result achievement stickers`로 커밋
- [x] `ROADMAP.md`와 이 문서 진행판을 완료 상태로 갱신

범위 비교와 승인 기준은 `references/COULD2_SCOPE_REVIEW.md`, 회색상자는 `references/COULD2_STICKER_PLACEMENT_REVIEW.md`, 흑백 앵커는 `references/COULD2_STICKER_ANCHOR_REVIEW.md`, 최종 결과는 `references/COULD2_STICKER_FINAL_REVIEW.md`를 따른다.

## 4. C3 — 로컬 2P 프로토타입

C2 완료 뒤 키보드+게임패드 한 화면 협동만 프로토타입한다. 기존 `GameScene`과 플레이 데이터는 단일 `player`를 전제로 하므로, 정규 1P 경로를 직접 바꾸지 않고 `?coopReview=1&debug=1` 전용 개발 장면과 시험 레벨에서 먼저 검증한다. 규칙 원문과 승인 기준은 `references/COULD3_COOP_RULES_REVIEW.md`를 따른다.

### 확정 제안 범위

- P1은 키보드, P2는 첫 번째 게임패드를 사용한다. 입력 장치의 동적 교환과 온라인 플레이는 제외한다.
- 실세아와 89% 구운 감자를 각각 한 명씩 고정 배정하며 플레이어끼리는 충돌하지 않는다.
- 별·목표·점수·체크포인트·비밀·월드 스위치·보스 HP는 공유하고, 체력·피격 무적·변신·비행 게이지는 개별 관리한다.
- 변신 아이템은 먼저 닿은 플레이어가 소유한다. 일반 수집물은 먼저 닿은 한 번만 공유 진행에 반영한다.
- 카메라는 두 플레이어의 중점을 따라가고, 허용 거리를 넘긴 후행 플레이어에게 경고 후 안전 지점 재합류를 적용한다.
- 적과 보스는 공격 시작 시 가장 가까운 유효 플레이어를 결정적으로 선택하고 해당 공격이 끝날 때까지 대상을 고정한다.
- 게이트는 두 플레이어가 함께 도착해야 통과하며, 사망·추락한 플레이어는 공유 체크포인트의 전용 오프셋으로 개별 부활한다.
- 프로토타입 완료·점수는 저장하거나 정규 해금에 반영하지 않는다. 새 캐릭터 아트·온라인·분할 화면은 C3 범위가 아니다.

### C3.1 범위·규칙 잠금

- [x] 단일 플레이어 결합 지점 조사: 입력, 장면, 카메라, 체크포인트, 체력, 변신, 환경, 적, 보스, UI
- [x] 입력 장치, 공유/개별 상태, 재합류, 부활, 아이템, 변신, 보스 타깃, 저장 격리 규칙 제안
- [x] 규칙 검토 문서와 구현 단계·승인 문구 작성
- [x] 사용자 규칙 승인 — 2026-09-07 사용자 `C3 규칙 승인`

### C3.2 순수 규칙·입력 경계

- [x] `src/data/coopPrototype.js`에 거리, 재합류, 대상 선택, 소유권, 게이트 판정 상수와 순수 함수 작성
- [x] 기존 `InputManager`의 1P 계약을 보존하고 P1 키보드/P2 pad 0을 독립 샘플링하는 `CoopInputManager` 작성
- [x] 게임패드 미연결·연결 해제 시 P1을 멈추지 않고 P2만 대기 상태로 전환
- [x] 동률 시 P1 우선, 공격 중 타깃 고정, 입력 edge 중복 방지 자동 검사

### C3.3 격리된 회색상자 장면

- [x] `c3-coop-test` 개발 레벨과 `CoopPrototypeScene`을 추가하고 `?coopReview=1&debug=1`에서만 진입
- [x] 기존 `Player`, `LevelLoader`, 캐릭터 에셋을 재사용해 실세아 P1과 감자 P2 생성
- [x] 두 플레이어용 지형 충돌·그림자·플레이어 번호 도형을 연결하고 플레이어 간 충돌은 만들지 않음
- [x] 정규 `GameScene`, 스테이지 선택, 진행 저장 경로가 변경되지 않았음을 구조 검사
- [x] 사용자 회색상자 승인 — 2026-09-07 사용자 `계속 진행`

### C3.4 공유 진행·개별 상태

- [x] 공유 체크포인트를 어느 플레이어든 활성화하고 부활 위치는 P1 -48px/P2 +48px로 분리
- [x] 플레이어별 HP·피격 무적·추락·부활과 공유 점수 감점을 연결
- [x] 별·일반 수집물·목표는 세션 공유 1회, 변신 아이템은 최초 접촉자 소유로 연결
- [x] 플레이어별 `TransformationManager`를 만들고 형태·비행·가드·무적 상태를 독립 관리

### C3.5 카메라·재합류

- [x] 활성 플레이어 중점을 사용하는 한 화면 카메라와 월드 경계 clamp 작성
- [x] 수평 820px 또는 수직 360px 초과 시 후행 플레이어 번호·방향 경고 표시
- [x] 수평 1000px 또는 수직 480px 초과가 1초 지속되면 후행 플레이어를 선행 플레이어 근처 안전 지점으로 재합류
- [x] 재합류 후 해당 플레이어에게만 1.2초 무적 적용
- [x] 좌→우·우→좌 후행 판정, 거리 동률, 연결 대기 플레이어 제외를 순수 함수로 검사

### C3.6 적·보스·게이트

- [x] 일반 적 타깃 센서가 가장 가까운 활성 플레이어를 선택하고 동률은 P1로 고정
- [x] 공유 보스 시험기가 공격 주기 동안 유효 타깃을 잠그고 주기 종료 뒤 결정적으로 재선정
- [x] 두 플레이어 모두 보스 시험기에 피해를 줄 수 있고 HP·격파·게이트 활성은 공유 1회
- [x] 센서·보스·가시 접촉 피해를 실제 충돌한 플레이어의 개별 세션 체력 어댑터로 전달
- [x] 두 플레이어 동시 게이트 겹침에서만 무저장 결과를 열고 다시 시험·메뉴 선택 제공

### C3.7 2P HUD·접근성

- [x] P1 원형 `1`, P2 마름모 `2` 표식을 캐릭터 위와 HUD에 함께 표시해 색상만으로 구분하지 않음
- [x] 플레이어별 HP·형태·비행 시간과 공유 점수·별·보스 HP를 한 화면에서 판독 가능하게 배치
- [x] 게임패드 대기·연결 해제·재연결, 거리 경고, 재합류, 부활, 아이템 소유자, 보스 타깃을 접근성 상태에 이름과 번호로 기록
- [x] `effects=reduced`, `mute=1`, `fallback=1`에서도 핵심 정보와 조작 계약 유지

### C3.8 검증·종료 게이트

- [x] 순수 규칙·구조·1P 회귀 자동 검사와 `npm run test`, `npm run validate`, `npm run build`
- [x] 1280×720 고정 상태에서 거리 경고, 재합류, 개별 HP·변신, 공유 수집, 적·보스 타깃, 동시 게이트 검수
- [ ] 실제 키보드+게임패드 1세트로 연결·해제·재연결과 전체 회색상자 완주
- [x] 미실시 실제 장치·어린이 테스트는 자동 검사로 완료 처리하지 않고 최종 리뷰에 기록
- [x] 사용자 최종 승인 — 2026-09-07 사용자 `C3 최종 승인`
- [x] 승인 범위만 `feat: prototype local co-op`로 커밋
- [x] `ROADMAP.md`와 이 문서 진행판을 완료 상태로 갱신

## 5. C4 — 추가 캐릭터 외형 슬롯

C3 완료 뒤 능력치가 동일한 외형 슬롯 하나를 데이터 기반으로 추가한다. 캐릭터 선택, 모든 필수 애니메이션, 변신·수영 파생 프레임, fallback과 저장 호환성을 먼저 검증하고 흑백 앵커와 최종 아트 승인 절차를 따른다.

## 6. 커밋 계획

| 단계 | 승인 후 커밋 메시지 |
|---|---|
| C1 | `feat: add rainbow relay challenge level` |
| C2 | `feat: add result achievement stickers` |
| C3 | `feat: prototype local co-op` |
| C4 | `feat: add character appearance slot` |
