# 반응형·새로고침·로컬 에셋 검증 보고서

> 실행일: 2026-08-10
> 자동 명령: `npm run test:release`

## 프로덕션 빌드와 로컬 에셋

개발 서버에서만 보이던 `/assets/...` 파일이 기존 배포 폴더에는 포함되지 않는 문제를 수정했다. 빌드 플러그인은 `assets/manifest.json`에 실제로 등록된 파일만 `dist/assets`로 복사하며, 런타임은 현재 문서의 `BASE_URL`을 기준으로 상대 URL을 만든다.

- manifest 런타임 파일: 101개
- 이미지·오디오 총용량: 2.91 MiB
- 외부 이미지·오디오 URL: 0개
- 배포 JS/CSS 진입점: 상대 경로 4개
- 하위 경로 배포 대응: `document.baseURI` + `import.meta.env.BASE_URL`

## 반응형 브라우저 측정

프로덕션 preview에서 동일한 실제 게임 구간을 열고 CSS 캔버스 크기, 내부 버퍼, 스크롤 영역을 측정했다.

| 조건 | 뷰포트 | 캔버스 | 위치 | 내부 버퍼 | 비율 |
|---|---:|---:|---:|---:|---:|
| 데스크톱 | 1280×720 | 1280×720 | x 0, y 0 | 1280×720 | 1.778 |
| 태블릿 | 1024×768 | 1024×576 | x 0, y 96 | 1280×720 | 1.778 |
| 모바일 세로 | 390×844 | 390×219.38 | x 0, y 312 | 1280×720 | 1.778 |
| 모바일 가로 | 844×390 | 693.33×390 | x 75, y 0 | 1280×720 | 1.778 |

네 조건 모두 문서 스크롤 크기가 뷰포트와 일치해 잘림이나 넘침이 없었다. 검사 중 CSS flex 정렬과 Phaser `CENTER_BOTH`가 중복되어 모바일 세로 캔버스가 아래로 밀리는 문제를 발견했고, Phaser만 중앙 정렬을 담당하도록 수정한 뒤 위 수치로 재검증했다.

## 새로고침

데스크톱 1280×720과 모바일 세로 390×844에서 실제 게임 직행 URL을 새로고침했다.

- 새로고침 전후 URL과 게임 구간 유지
- 접근성 상태 문구 정상 복원
- 캔버스 내부 버퍼 1280×720 유지
- 브라우저 오류 로그 0건

## 범위

`오프라인 에셋`은 게임 이미지·오디오가 외부 CDN이나 서버에 의존하지 않고 프로덕션 빌드에 포함된다는 의미로 검증했다. 설치형 PWA나 서버가 완전히 꺼진 상태의 오프라인 탐색은 현재 요구 범위에 포함하지 않는다.
