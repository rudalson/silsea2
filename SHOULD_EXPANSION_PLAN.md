# Should 확장 실행 계획

> 문서 상태: S1~S6 사용자 승인 및 구현 완료
> 최근 갱신: 2026-09-04

## 0. 재개 규칙

1. `ROADMAP.md`의 현재 단계와 이 문서의 진행판을 확인한다.
2. S1~S6 중 첫 미완료 단계의 첫 미완료 체크박스부터 작업한다.
3. 각 단계는 회색 상자 또는 기술 구현 → 자동 검사 → 브라우저 확인 → 사용자 승인 → 커밋 순서로 닫는다.
4. 사용자 승인 전에는 다음 Should 단계에 착수하지 않는다.
5. 어린이 플레이테스트 미실시 예외는 실제 수행으로 바꾸지 않는다. 새 기능은 별도 자동·수동 검증 근거를 남긴다.

## 1. 진행판

| 단계 | 범위 | 상태 | 승인 문구 |
|---|---|---|---|
| S1 | 비밀 공간과 별 발견 보상 | 승인 완료 — 2026-09-03 | `S1 비밀공간 승인` |
| S2 | 선택 목표 결과 축하 연출 | 승인 완료 — 2026-09-03 | `S2 결과연출 승인` |
| S3 | 레벨 데이터 핫 리로드 | 승인 완료 — 2026-09-03 | `S3 핫리로드 승인` |
| S4 | 디버그 패널 preset | 승인 완료 — 2026-09-03 | `S4 프리셋 승인` |
| S5 | 지형별 가벼운 발소리 | 승인 완료 — 2026-09-04 | `S5 발소리 승인` |
| S6 | 추가 배경 장식 4종 | 승인 완료 — 2026-09-04 | `S6 최종 승인` |

## 2. S1 — 비밀 공간과 별 발견 보상

목표: 기존 안전 경로를 막지 않으면서 별 궤적을 따라 상단·우회 경로를 탐색하면 명확한 발견 피드백과 선택 목표를 제공한다.

### 잠금 전 회색 상자 규칙

- 플레이 레벨 5개에 비밀 공간을 1곳씩 둔다.
- 비밀 공간은 필수가 아니며 기존 지상 진행과 보스 클리어를 바꾸지 않는다.
- 별 또는 별 아크가 진입 방향을 안내하고, 공간 안에는 대형 퍼센트 보상을 둔다.
- 최초 진입 때만 발견 보너스 200점을 지급하고 같은 세션의 재진입·디버그 재구성으로 중복 지급하지 않는다.
- `find_secrets: 1`을 선택 목표로 기록하고, 발견 이름·`+200`·진행 수를 HUD와 접근성 상태에 표시한다.
- 신규 최종 아트·오디오는 만들지 않고 승인된 별·대형 퍼센트·기존 SFX를 재사용한다.

### 데이터 계약

`secrets[]` 항목은 `id`, `name`, `xStart`, `xEnd`, `yTop`, `yBottom`, `reward`, `guideItemIds`, `rewardItemId`를 가진다. 구역은 월드 안의 양수 크기 사각형이어야 하고, 단서는 같은 레벨의 `star`/`star_arc`, 물리 보상은 `percent_large`여야 한다.

### 레벨 배치

| 레벨 | 비밀 공간 | 기존 선택 경로 | 발견 보상 |
|---|---|---|---|
| `level-01` | 회복길 하늘 정원 | `secret_sky_arc` 상단 | 대형 퍼센트 + 발견 200 |
| `level-02` | 별나무 꼭대기 | `star_tree_arc` 상단 | 대형 퍼센트 + 발견 200 |
| `level-03` | 안개 속 등불길 | 응용 구간 별 아크 | 대형 퍼센트 + 발견 200 |
| `level-04` | 파도 위 지붕 전망대 | 높은 대피 지형 | 대형 퍼센트 + 발견 200 |
| `level-05` | 잠긴 마을 종탑 | 회복 구간 상단 별 아크 | 대형 퍼센트 + 발견 200 |

### 구현·검증 체크리스트

- [x] `SecretManager` 1회 발견·점수·선택 목표·이벤트 구현
- [x] HUD 토스트, 접근성 문구, 기존 대형 퍼센트 SFX 연결
- [x] 플레이테스트 `secretsFound` 이벤트·분석 합계 추가
- [x] 다섯 레벨 `secrets[]`, 별 단서, 대형 퍼센트와 `find_secrets` 배치
- [x] 비밀 구역 범위·ID·단서·보상·목표 count 검증 추가
- [x] 1회 보상·구역 밖 미발견·목표 완료 단위 테스트
- [x] 다섯 레벨 debug 고정 상태와 콘솔 warning/error 0건 확인
- [ ] 키보드로 지상 경로를 유지한 채 실제 상단 경로 진입·복귀 확인
- [ ] 게임패드와 쉬운 모드에서 실제 진입 확인
- [x] 사용자 회색 상자 승인 — 2026-09-03 `S1 비밀공간 승인`

검수 주소와 화면은 `references/SHOULD1_SECRET_GRAYBOX_REVIEW.md`를 따른다. 200점, 구역 크기와 배치는 2026-09-03 사용자 승인으로 잠갔다. 키보드·게임패드 실제 진입 항목은 수행 완료로 바꾸지 않고 후속 회귀 검사로 남긴다.

## 3. S2 — 선택 목표 결과 축하 연출

### 잠금 전 회색 상자 규칙

- `ClearScene`은 이번 플레이에서 달성한 목표를 레벨의 선택 목표 순서대로 최대 3개 카드로 표시한다.
- `collect_stars`, `find_secrets`, `clear_time`, `no_damage`는 아이콘·이름·정확한 조건·달성 표식을 함께 쓴다.
- 4개 달성은 전체 개수와 `대표 3개` 문구로 알리고, 달성 0개는 실패 표현 대신 다음 도전 안내를 보여준다.
- 기본 효과는 260ms 간격 이동·확대·페이드, 효과 약하게는 180ms 간격의 짧은 페이드만 사용한다.
- 카드는 전달된 `achieved`를 읽기만 하며 점수·최고 기록·달성 저장을 다시 변경하지 않는다.

### 구현·검증 체크리스트

- [x] 4종 선택 목표 프레젠테이션 데이터와 조건 문구 구현
- [x] 최대 3개 순차 카드, 4개 달성 요약과 0개 안내 상태 구현
- [x] 클리어 화면 접근성 전체 요약과 색에 의존하지 않는 달성 표식 구현
- [x] 화면 효과 약하게·음소거·에셋 fallback 상태 구현
- [x] 직접 결과 화면 검수 진입점과 presentation coverage 검증 추가
- [x] 정상·순차 중간 프레임·약한 효과/fallback·0개 상태 콘솔 warning/error 0건 확인
- [ ] 키보드로 다음 스테이지와 스테이지 선택 재진입 확인
- [ ] 실제 게임패드로 두 재진입 경로 확인
- [x] 사용자 회색 상자 승인 — 2026-09-03 `S2 결과연출 승인`

검수 주소와 화면은 `references/SHOULD2_OBJECTIVE_CELEBRATION_REVIEW.md`를 따른다. 카드 순서·문구·260/180ms 간격과 최대 3개 규칙은 2026-09-03 사용자 승인으로 잠갔다. 키보드·실제 게임패드 재진입은 수행 완료로 바꾸지 않고 후속 회귀 검사로 남긴다.

## 4. S3 — 레벨 데이터 핫 리로드

### 확정 구현 규칙

- `npm run dev`와 `?debug=1`을 함께 사용한 현재 `GameScene`에서만 `레벨 데이터 다시 읽기` 버튼을 활성화한다.
- Vite가 현재 레벨 모듈 변경을 감지하면 패널은 `새 데이터 준비됨 · revision N`을 표시하되, 사용자가 버튼을 누르기 전에는 실행 중인 레벨을 바꾸지 않는다.
- 재로드는 먼저 스키마, 레벨 ID, 월드·구간 범위, 기존 에셋 키와 로드된 tilemap을 검증한다. 하나라도 실패하면 현재 실행본을 유지하고 `적용하지 않음 · <원인>`을 표시한다.
- 성공 시 위치·속도·중력 상태, 선택 캐릭터, 변신·비행 시간, HP, 일반/쉬운 모드, 점수·콤보·목표·체크포인트 진행, 발견한 비밀 공간, 이미 수집한 아이템과 처치한 적을 유지한다.
- 지형·적·아이템·보스와 이에 연결된 collider·overlap·listener·timer·pool은 새 후보 레벨을 완전히 만든 뒤 한 번 교체한다. 후보 생성 실패 시 기존 월드와 manager를 건드리지 않는다.
- 이미지·오디오·tilemap 에셋 키 변경은 핫 리로드 범위가 아니며 전체 레벨 재시작으로 적용한다.
- 진행 중인 보스의 부분 HP·phase·공격 순서는 새 보스 데이터 확인을 위해 초기화한다. 이미 격파한 보스와 열린 게이트는 유지한다.
- Scene 종료 시 모듈 구독과 제어기를 해제한다. 프로덕션 번들에는 Vite 개발 클라이언트나 HMR context를 포함하지 않는다.

### 구현·검증 체크리스트

- [x] 개발 전용 레벨 모듈 HMR 경계와 revision 구독 구현
- [x] 사전 검증·준비·적용·실패 보존을 분리한 `LevelHotReloadController` 구현
- [x] 디버그 패널 준비/로딩/성공/오류 상태와 접근성 status 구현
- [x] 플레이어·난이도·진행 상태를 보존하는 트랜잭션형 런타임 교체 구현
- [x] 동일 데이터와 실제 파일 변경 후 적용, 잘못된 데이터 거부를 브라우저에서 확인
- [x] 실제 레벨 재로드 100회 뒤 collider/pool/FX 상한과 console warning/error 0건 확인
- [x] 제어기 100회·오류 보존·dispose 단위 검사와 프로덕션 HMR 코드 부재 검사
- [x] 사용자 기술 동작 승인 — 2026-09-03 `S3 핫리로드 승인`

검수 주소·절차·화면과 자동 검사 근거는 `references/SHOULD3_LEVEL_HOT_RELOAD_REVIEW.md`를 따른다. 검수용 임시 유효/무효 데이터 변경은 모두 원복했으며 레벨 원본에는 남아 있지 않다.

## 5. S4 — 디버그 패널 preset

### 확정 구현 규칙

- `이동 기본`, `초보`, `공중 제어 검수` 세 preset을 동결된 읽기 전용 데이터로 관리한다.
- preset을 선택해도 즉시 적용하지 않는다. 현재값과 목표값이 다른 항목을 `현재 → 목표` 목록으로 먼저 보여주고 `선택값 적용`을 눌러야 반영한다.
- 적용 대상은 중력, 점프 초기 속도, Jump Cut, 지상 가속·감속, 최대 이동 속도, 공중 가속도, Coyote Time, Jump Buffer 아홉 값이다.
- preset은 현재 `tuning` 객체만 바꾸며 `easyMode`, 진행도 저장, 캐릭터 원본 데이터와 브라우저 저장소를 변경하지 않는다.
- `기본값`은 패널 진입 때의 캐릭터 기본값을 복원하고, `JSON 내보내기`는 현재 적용값을 계속 내보낸다.
- 슬라이더·select·button의 키 입력이 Phaser 게임 조작으로 전달되지 않게 막는다. 방향키로 preset을 고르고 Enter/Space 또는 포인터로 적용할 수 있어야 한다.
- S3 레벨 데이터 재로드 뒤에도 선택 preset과 현재 튜닝값을 유지한다. 새 게임 진입 때는 캐릭터 기본값으로 시작한다.

### preset 값

| 항목 | 이동 기본 | 초보 | 공중 제어 검수 |
|---|---:|---:|---:|
| 중력 | 1900 | 1700 | 1800 |
| 점프 초기 속도 | -720 | -740 | -760 |
| Jump Cut | 0.45 | 0.55 | 0.50 |
| 가속도 | 2300 | 2800 | 2300 |
| 감속도 | 3000 | 3600 | 3000 |
| 최대 이동 속도 | 360 | 320 | 360 |
| 공중 가속도 | 1100 | 1400 | 1900 |
| Coyote Time | 120 | 180 | 150 |
| Jump Buffer | 120 | 180 | 150 |

### 구현·검증 체크리스트

- [x] 세 preset과 아홉 컨트롤을 읽기 전용 데이터로 분리
- [x] 선택 설명, 적용 전 변경 목록, 명시적 적용과 적용 완료 상태 구현
- [x] 공중 가속도 슬라이더와 preset 적용 뒤 모든 입력·출력 동기화 구현
- [x] 기본값 복원·JSON 내보내기·S3 레벨 재로드 뒤 값 유지 확인
- [x] 방향키 선택·Enter 적용과 포인터 선택·적용 확인
- [x] `?debug=1` 없는 일반 실행에서 패널·preset 미노출 확인
- [x] preset 범위·step·불변성·미리보기 무변경·적용 단위 검사
- [x] 사용자 preset 값·변경 목록·조작 승인 — 2026-09-03 `S4 프리셋 승인`

검수 주소·화면과 재현 절차는 `references/SHOULD4_DEBUG_PRESET_REVIEW.md`를 따른다.

## 6. S5 — 지형별 가벼운 발소리

### 확정 구현 규칙

- 표면 키는 `grass`, `dirt`, `stone`, `wood`, `shallow_water` 다섯 값으로 제한한다. Tiled `terrain` 레이어의 `groundSurface`·`platformSurface`를 기본값으로 쓰고, 다른 재질인 오브젝트만 `surface` 속성으로 덮어쓴다.
- 현재 플레이어 발 위치와 수평으로 겹치고 높이 차가 24px 이내인 가장 가까운 지형 오브젝트를 지지면으로 판정한다. 표면 값이 없거나 허용 목록 밖이면 발소리를 생략한다.
- 달리기 8프레임 중 발이 지면에 닿는 0·4번 프레임에서만 재생한다. 같은 프레임을 여러 update가 관찰해도 접촉당 한 번만 요청한다.
- 접지 상태와 40px/s 이상의 수평 속도를 모두 요구한다. 점프/낙하, 수영, 피격 애니메이션·조작 잠금 중에는 재생하지 않는다.
- 발소리 자체 볼륨은 0.34로 고정하고 최종 출력은 기존 SFX 볼륨과 음소거 설정을 곱한다. 없는 키·디코딩 실패는 무음으로 진행하며 동일 키 프레임당 최대 2회 제한은 `AudioManager`를 그대로 사용한다.
- 디버그 패널에 현재 표면, 실제 재생 횟수, 억제·fallback 상태를 표시한다. 레벨 핫 리로드 시 새 tilemap 표면 데이터로 매니저를 함께 교체한다.

### 구현·검증 체크리스트

- [x] 5개 표면 키와 레이어 기본값·오브젝트 덮어쓰기 데이터 계약 구현
- [x] 실제 달리기 접촉 프레임 감지와 동일 접촉 중복 제거 구현
- [x] 점프·수영·피격 잠금·저속 상태 억제와 무음 fallback 연결
- [x] 재질별 WAV 5종 생성, manifest·오디오 목록·크레딧 등록
- [x] 레벨·구조·핵심·에셋·통합·빌드 자동 검증 통과
- [x] 브라우저에서 5개 표면 재생, 점프·수영 억제와 Vite 오류 오버레이 0건 확인
- [x] 사용자 5종 청취·최종 승인 — 2026-09-04 `S5 발소리 승인`

검수 주소·절차·청취 파일은 `references/SHOULD5_FOOTSTEP_REVIEW.md`를 따른다.

## 7. S6 — 추가 배경 장식 4종

### 배치안

| 키 | 역할 | 레벨·구간 | 월드 배치 | 표시 크기·깊이 |
|---|---|---|---|---|
| `decor_grass` | 튜토리얼 지면과 첫 발판 사이의 낮은 식생 리듬 | `level-01` · `tutorial` | x 640, y 578 | 144×72, depth -5 |
| `decor_flower` | 유니콘 정원 중간의 부드러운 구간 표식 | `level-01` · `unicorn_garden` | x 3200, y 578 | 88×88, depth -4 |
| `decor_rock` | 첫 별과 첫 안개 단서 사이의 낮은 계곡 소재 | `level-03` · `mist_intro` | x 576, y 578 | 112×72, depth -5 |
| `decor_sign` | 역방향 스테이지 시작을 재확인하는 문자 없는 왼쪽 화살표 | `level-04` · `tsunami_intro` | x 9920, y 578, `flipX: true` | 112×144, depth -4 |

### 확정 규칙

- 총 4개 파일을 각 1회만 배치한다. 이미 전용 장식 9개가 있는 `level-02`, 수면·숨 경계가 우선인 `level-05`, 모든 보스룸에는 추가하지 않는다.
- 투명 단일 PNG 월드 소품으로 만들고 지형에 고정하므로 `scrollFactor`는 1을 유지한다. 기존 near 배경보다 앞, 충돌 지형·캐릭터보다 뒤인 depth -5~-4에 둔다. seamless 규칙은 단일 투명 소품이므로 적용하지 않는다.
- 수집물의 `collect` 고채도와 위험물의 뾰족한 실루엣을 쓰지 않는다. 꽃은 낮은 채도의 분홍·황토, 풀은 환경 녹색, 바위는 중성 회색, 표지는 목재색만 사용한다.
- 꽃·풀·바위는 충돌·상호작용·애니메이션·후광이 없는 순수 배경 장식이다. 표지판도 충돌과 문자가 없고 오른쪽 원본을 런타임에서 좌우 반전해 진행 방향을 표시한다.
- 배치 중심에서 캐릭터·별·체크포인트·적·위험물의 핵심 실루엣과 겹치지 않으며 화면 중앙 상단을 비운다.
- 배치 승인 뒤 4종을 한 장의 흑백 앵커로 먼저 검수하고, 앵커 승인 뒤에만 최종 PNG·manifest·mapping·레벨 데이터를 연결한다.

### 구현·검증 체크리스트

- [x] 기존 화면·동선·아이템·위험물 좌표를 대조해 4종 역할과 최소 배치안 작성
- [x] 1280×720 대표 화면에서 후보 지점과 중앙 가독성 확인
- [x] 파일 크기·표시 크기·팔레트 역할·깊이·반전 규칙 문서화
- [x] 사용자 배치 승인 — 2026-09-04 사용자 `S6 배치 승인`
- [x] 4종 흑백 앵커·25% 실루엣 제작
- [x] 사용자 앵커 승인 — 2026-09-04 사용자 `S6 앵커 승인`
- [x] 승인 앵커 기반 최종 PNG 4종 1회 생성
- [x] manifest·mapping·레벨 데이터·`flipX` 런타임 연결
- [x] 1280×720·25% 축소·fallback·release 검증
- [x] 사용자 최종 승인 — 2026-09-04 사용자 `S6 최종 승인`

배치 근거 화면과 후속 제작 게이트는 `references/SHOULD6_DECOR_PLACEMENT_REVIEW.md`를 따른다.

## 8. 커밋 계획

| 단계 | 승인 후 커밋 메시지 |
|---|---|
| S1 | `feat: add secret discovery rewards` |
| S2 | `feat: celebrate optional objectives` |
| S3 | `dev: add level data hot reload` |
| S4 | `dev: add debug tuning presets` |
| S5 | `feat: add terrain footsteps` |
| S6 | `feat: add optional background decorations` |
