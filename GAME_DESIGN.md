# 게임 디자인 문서

> 최초 Phase 0 승인 · 2026-08-03 / 신규 스테이지 확장 P0 승인 · 2026-08-25

## 게임 한 문장

귀여운 망아지 캐릭터로 달리고 점프하며 아이템을 얻어 유니콘·페가수스·알리콘으로 변신하고, 각 능력으로 장애물과 적을 돌파해 퍼센트 점수를 폭발적으로 높이는 2D 횡스크롤 액션 게임.

## 플레이어와 목표

- 대상: 플랫폼 게임을 처음 접하는 어린이를 포함한 전 연령.
- 기준 화면: 1280×720, 브라우저 크기에 맞추되 종횡비를 보존한다.
- 플레이 캐릭터: `실세아`, `89% 구운 감자` 중 하나를 선택한다.
- 두 캐릭터는 Phase 0에서 외형만 다르고 이동·변신 능력은 동일하다. 원안에 없는 능력 차이는 추가하지 않는다.
- 필수 승리 조건: 감자 대왕 격파 후 생성되는 무지개 게이트 도달.
- 선택 목표: 별 수집, 제한 시간, 무피해. 점수와 최고 기록은 레벨별로 저장한다.

## 디자인 원칙

1. 역할은 색과 흑백 실루엣만으로 읽힌다.
2. 화면 밖 공격은 활성화하지 않는다.
3. 실패 후 메뉴를 거치지 않고 체크포인트에서 빠르게 재개한다.
4. 변신 직후 새 능력을 바로 써 볼 안전한 상황을 제공한다.
5. 5~10초마다 아이템, 적, 지형, 변신, 비밀 공간 중 하나의 보상을 배치한다.
6. 새 능력은 소개 → 연습 → 응용 → 조합 순서로 텍스트 없이 가르친다.

## 게임 흐름

`부트 → 메뉴 → 캐릭터 선택 → 스테이지 선택 → 게임 → 클리어 결과 → 스테이지 선택`

- 게임 오버 화면은 없다.
- 일시정지에서 조작 안내와 접근성 설정에 진입한다.
- 저장 실패 시 메모리 진행도로 계속 플레이한다.

### 확장 스테이지 순서

| 순서 | 스테이지 | 핵심 경험 |
|---:|---|---|
| 1 | 무지개 언덕 | 이동·변신·기존 적·감자 대왕 학습 |
| 2 | 별빛 숲 | 달빛 나뭇가지와 공중 이동 |
| 3 | 안개 골짜기 | 안개 속 시야와 길 찾기 |
| 4 | 쓰나미 마을 | 오른쪽에서 오는 파도를 피해 왼쪽으로 달리는 역방향 추격 |
| 5 | 물에 잠긴 마을 | 다른 침수 마을을 잠수해 지나며 숨 관리 |

- 스테이지 선택은 가운데 카드와 양옆 일부가 보이는 좌우 캐러셀을 사용한다.
- 첫 스테이지만 처음부터 열고, 직전 스테이지를 클리어하면 다음 스테이지를 해금한다.
- 잠긴 카드도 이름과 미리보기 실루엣을 보여 다음 목적지를 알 수 있게 한다.
- 해금 여부는 별도 저장 필드가 아니라 직전 `order` 레벨의 `cleared` 값으로 계산한다.
- 클리어 화면에서는 다음 스테이지로 바로 진행하거나 선택 화면으로 돌아갈 수 있다.

## 핵심 플레이 루프

1. 달리고 점프해 새 구간을 발견한다.
2. 별과 퍼센트 아이템을 모아 점수를 올린다.
3. 변신 아이템을 얻는다.
4. 자석·비행·피버 능력으로 장애물을 돌파한다.
5. 적 처치·비밀 공간·콤보로 큰 보상을 얻는다.
6. 체크포인트에서 회복한 뒤 한 단계 어려운 응용 구간으로 간다.

## 이동과 점프

| 항목 | Phase 1 시작값/규칙 |
|---|---|
| 좌우 이동 | 가속·감속 적용, 방향 전환은 빠르게 |
| 공중 제어 | 지상보다 약한 가속 |
| Coyote Time | 120ms |
| Jump Buffer | 120ms |
| Jump Cut Multiplier | 0.45 |
| 최대 낙하 속도 | 900px/s |
| Fall Gravity | 상승 중력보다 강하게, 수치는 디버그 패널에서 확정 |
| 충돌 상자 | 시각 크기보다 15~25% 작게, 몸통 중심 |
| Head Bump | 천장 충돌 시 위치를 분리해 지형 끼임 방지 |

- 점프, 착지, 피격 squash & stretch는 origin `(0.5, 1.0)`에서 코드 스케일로만 구현한다.
- 중력, 점프 속도, 점프 컷, 가속, 감속, 최대 속도, Coyote Time, Jump Buffer는 디버그 슬라이더와 JSON 내보내기를 제공한다.
- 입력은 `InputManager`에만 등록한다.

## 입력

| 행동 | 키보드 | 게임패드 |
|---|---|---|
| 이동 | 방향키 / WASD | D-pad / 왼쪽 스틱 |
| 점프·비행 | Space / Z | A |
| 특수 행동 | Shift / X | X |
| 일시정지 | Esc | Menu/Start |

## 변신

### 기본형

- 실세아 기본형에는 뿔과 날개가 없다.
- 두 캐릭터 모두 일반 달리기와 점프만 사용한다.

### 유니콘

- 뿔 아이템으로 진입한다.
- 가까운 별을 자동으로 끌어온다.
- 가시 호박을 안전하게 파괴할 수 있다.
- 반원 별 배치로 자석 범위를 시각적으로 가르친다.

### 페가수스

- 날개 아이템으로 진입한다.
- 점프 입력을 유지하면 비행한다.
- 최대 비행 시간 10초, 지면에서 약 3초 동안 완전 회복한다.
- 체크포인트는 게이지를 즉시 채운다.
- 소진 시 즉시 추락하지 않고 짧은 활공으로 전환한다.
- `Shift/X` 또는 게임패드 X를 누르면 120ms 준비 뒤 정면 150°·96px 범위의 등록 투사체를 날개로 막는다.
- 방어는 비행 게이지를 초당 1.5초분 소모하고, 이동 속도를 55%로 제한하며, 방어 중 상승 비행은 하지 않는다.
- 방어 해제 뒤 350ms 동안 다시 발동할 수 없다. 뒤에서 온 투사체와 레이저·번개·접촉·환경 피해는 막지 못한다.

### 알리콘 피버

- 무지개 뿔날개 아이템으로 12초간 진입한다.
- 무제한 비행, 넓은 자석, 일반 적·장애물 접촉 파괴, 완전 무적, 점수 2배를 제공한다.
- 종료 3초 전 HUD와 효과음으로 예고한다.
- 종료 시 마지막 안전 지점을 사용해 위험 지형 안에서 상태가 해제되지 않게 한다.
- 보스는 공격을 무시하기 쉬워질 뿐, HP 3과 요구 타격 횟수를 유지한다.

### 확장 환경에서의 변신 규칙

| 형태 | 쓰나미 | 수중·숨 |
|---|---|---|
| 기본형 | 안전지대로만 회피 | 표준 숨 규칙 적용 |
| 유니콘 | 별도 대응 수단 없음 | 자석 성능과 숨 규칙 모두 기존 기준 유지 |
| 페가수스 | 비행으로 파도를 넘을 수 있음 | 물속에서는 비행 불가·비행 게이지 소모 없음, 수면 위에서만 비행 |
| 알리콘 | 파도 피해 무시 | 숨 소모·익사 피해 무시 |

- 쓰나미·침수 핵심 구간에는 알리콘 아이템을 배치하지 않는다.
- 신규 환경에 들어갔다는 이유만으로 유니콘 자석 반경 등 기존 능력 수치를 숨겨서 낮추지 않는다.

## HP, 피격, 추락, 부활

- 기본 최대 HP는 캐릭터 데이터에서 정하고, 두 캐릭터는 같은 시작값을 쓴다. 쉬운 모드는 최대 HP +2다.
- 피격 시 HP 1 감소, 경직 250ms 이하, 2초 무적, 짧은 넉백과 깜빡임을 적용한다.
- 낭떠러지는 HP 1을 줄이고 마지막 안전 지점으로 복귀시킨다.
- HP 0이면 마지막 체크포인트에서 HP를 완전히 회복하고, 일부 점수만 잃은 뒤 2초 무적으로 재개한다.
- 체크포인트가 없으면 스테이지 시작점으로 돌아간다.
- 까치의 점수 절도량은 `min(현재 점수 × 10%, 100)`이며 일부를 즉시 회수 가능한 아이템으로 흩뿌린다.

- 쓰나미 피격은 HP 1을 줄이고 같은 파도에서 중복 피해를 주지 않는다. 파도 방향과 같은 왼쪽으로 짧게 밀되 파도 쪽으로 되밀지 않는다.
- 숨 게이지가 0이 되면 즉사하지 않고 일반 2.5초, 쉬운 모드 3.5초마다 HP 1을 잃는다.
- 익사 피해는 기존 HP 0·체크포인트·`no_damage` 기록 경로를 사용하되 넉백과 경직은 적용하지 않는다.
- 체크포인트 부활 시 숨을 완전히 회복하고, 쓰나미는 일반 3초·쉬운 모드 4.5초 동안 다시 오지 않는다.
- 물 바닥은 추락으로 처리하지 않으며 침수 구간 안에 `pit`을 두지 않는다. 기존 월드 밖 추락 구조는 안전장치로 유지한다.

## 확장 환경 기믹

### P6 공용 전투·장치

- 궁수는 `대기 → 조준 예고 → 1발 발사 → 빈틈` 순서로 행동하고 화면 밖에서는 공격하지 않는다.
- 화살과 이후 일반 투사체는 데이터의 `guardable: true`가 있을 때만 날개 방어 대상이다. 감자 대왕 탄환은 기존 보스 밸런스를 유지하기 위해 기본값을 방어 불가로 둔다.
- 레이저는 `예고 → 빔 → 휴지`를 반복하며 날개로 막지 못한다. 스위치 접촉이나 예고를 보고 통과하는 두 선택이 모두 유효해야 한다.
- 레이저 스위치는 같은 레벨의 같은 `switchId` 빔만 끈다. 체크포인트 부활 뒤에는 유지하고 스테이지 이탈·처음부터 다시 시작할 때 초기화한다.
- 궁수와 레이저는 날개 아이템 뒤의 별빛 숲에서 `소개 → 연습 → 응용 → 조합` 순서로 처음 배치한다. 기존 낭떠러지에는 새 투사체·레이저를 추가하지 않는다.


- 신규 스테이지의 목표 완주 시간은 일반·쉬운 모드 모두 각 4~6분이며, 선택 목표 `clear_time`은 목표 상한보다 30초 길게 둔다.
- 쓰나미·익사로 받은 HP 피해는 선택 목표 `no_damage` 실패에 포함한다.

### 안개 골짜기

- 안개는 길을 완전히 숨기는 암기 장치가 아니라, 실루엣·밝기·움직임 중 두 가지 이상의 단서로 다음 진행 방향을 찾게 하는 장치다.
- 소개 → 연습 → 응용 → 조합 → 회복 순으로 농도와 지형 복잡도를 높인 뒤 시야를 회복시킨다.
- 2026-08-26 승인 수치는 소개 `26% / 430px`, 연습 `42% / 360px`, 응용 `54% / 320px`, 조합 `62% / 280px`, 회복 `18% / 500px`다. 각 값은 `안개 농도 / 플레이어 주변 가시 반경`이다.
- 모든 안개 영역에는 밝기+세로 실루엣의 빛 기둥과 밝기+이동의 바람 화살표를 각각 하나 이상 둔다.
- 화면 효과 강도를 `약하게`로 두어도 같은 코스를 완주할 수 있어야 한다.
- `약하게`는 농도에 0.55배를 적용하고 가시 반경을 70px 늘린다. 쉬운 모드는 농도에 0.82배, 가시 반경에 1.15배를 적용한다.
- 중간 보스 외형·패턴·보상은 영상과 단계별 사용자 승인 전에는 추측하지 않는 원칙으로 제작했다. 안개 골짜기에는 승인된 투명 대왕을 최종 보스로 연결한다.

### 쓰나미 마을

- 플레이어는 월드 오른쪽에서 시작해 왼쪽 출구로 진행하고, 쓰나미도 오른쪽에서 왼쪽으로 반복 이동한다.
- 집 내부, 언덕 뒤, 높은 지형은 파도를 완전히 피하는 안전지대다. 기본형도 안전지대만으로 무피해 완주할 수 있어야 한다.
- 첫 회색 상자는 다음 승인 기준값으로 시작한다.

| 항목 | 일반 모드 | 쉬운 모드 |
|---|---:|---:|
| 첫 경고 | 시작 후 6초 이상 | 시작 후 8초 이상 |
| 시각·음향 예고 | 1.5초 | 2.2초 |
| 반복 간격 | 9~12초 | 13~16초 |
| 파도 속도 | 플레이어 최고 속도의 1.15배 | 1.05배 |
| 통과 지속 시간 | 2.5초 | 3.0초 |
| 안전지대 인정 유예 | 도달 0.25초 전까지 | 0.4초 전까지 |

- 파도가 통과하는 동안 적의 공격과 이동을 정지한다.
- 파도는 한 번의 연출이 아니라 스테이지 전체의 추격 리듬이다.

### 물에 잠긴 마을

- 쓰나미 마을과 다른 마을이지만, 플레이어가 재해 지역을 아직 벗어나지 못했다는 이야기로 연결한다.
- 머리 상단이 `surfaceY` 아래로 완전히 잠기면 숨이 줄고, 수면보다 8px 이상 위로 올라와야 회복한다. 물속 점프만으로는 회복하지 않는다.
- 다음 호흡 지점은 잠수 전에 화면에서 읽을 수 있어야 한다.
- 첫 회색 상자는 다음 승인 기준값으로 시작한다.

| 항목 | 일반 모드 | 쉬운 모드 |
|---|---:|---:|
| 숨 완전 소모 | 연속 잠수 12초 | 연속 잠수 17초 |
| 수면 완전 회복 | 2초 | 1.5초 |
| 숨 부족 경고 | 잔량 30% | 잔량 35% |
| 물속 중력 배수 | 0.35 | 0.35 |
| 물속 최대 낙하 속도 | 260px/s | 260px/s |
| 물속 좌우 최대 속도 | 지상의 0.75배 | 지상의 0.8배 |
| 상승 입력 | -230px/s·0.35초 간격 | -240px/s·0.3초 간격 |

### 재난 소재 표현

- 마을은 비어 있고 물에 잠긴 상태로만 표현하며 피해자·인명·잔해 묘사를 하지 않는다.
- 파도 피격은 기존 `hurt`와 짧은 밀림으로 처리하고, 떠내려가거나 가라앉는 연출을 만들지 않는다.
- 숨 0은 괴로움이나 질식 묘사 대신 “수면으로 올라가기” 안내로 표현한다. 비명과 화면 암전을 사용하지 않는다.
- 경고는 공포보다 방향과 타이밍 안내를 우선하고 실제 재난 사건을 연상시키는 이름·문구를 쓰지 않는다.

## 점수와 콤보

- HUD의 퍼센트 점수는 목표값까지 카운트업한다.
- 별, 작은 퍼센트, 큰 퍼센트, 적 처치, 비밀 공간, 선택 목표가 점수를 준다.
- 정확한 기본 배점과 콤보 창은 Phase 2 데이터 튜닝 항목으로 두며 Scene에 하드코딩하지 않는다.
- 알리콘 중 획득분은 2배다.
- 연속 별 수집은 4~8단계 상승 음계와 시각 콤보를 제공하고, 간격이 끊기면 초기화한다.
- 각 플레이 레벨은 필수 진행을 막지 않는 상단·우회 비밀 공간을 1곳 가진다. 별 궤적이 입구를 암시하고 공간 안의 대형 퍼센트와 최초 발견 200점으로 탐색을 보상한다.
- 비밀 공간 발견은 세션당 한 번만 인정하며 이름·점수·발견 수를 HUD와 접근성 문구로 함께 알린다. `find_secrets`는 선택 목표이며 보스·게이트 완료 조건이 아니다.

## 적과 보스

| 대상 | 핵심 행동 | 최소 예고 |
|---|---|---|
| 데굴데굴 생감자 | 느린 지상 순찰·구르기, 밟기 가능 | 접촉형, 화면 안에서만 활성 |
| 가시 호박 | 고정 위험물, 유니콘/알리콘으로 파괴 | 300ms 이상 warning |
| 먹구름 | 위치 그림자 후 번개 | 700ms |
| 까치 | 흔들림+시선+표식 후 급강하, 이후 기절 | 600ms 이상 |
| 감자 대왕 | 점프 지진과 수평 탄환, 3단계 | 700~1,000ms |
| 훌라후프 대왕 (`level-04`) | 회전 중 방어, 1발→교차 2발→양방향 훌라후프 뒤 정지 약점 | 900ms |
| 투명 대왕 (`level-03`) | 빛기둥으로 위치 공개→기억→투명화, 틀린 위치를 밟으면 범위 공격 | 900ms |
| 물 대왕 (`level-05`) | 네 웅덩이 사이 잠수 이동, 1→2→3발 물 공격 뒤 5→4→3초 약점 | 900~1,000ms |
| 랜덤 대왕 (`level-02`) | 재도전·점수 ±100·전투 결과 뒤 네 공격 중 Seed 덱 선택 | 800~1,000ms |

- 모든 공격 예고는 색 변화·자세 변화·그림자·효과음 중 두 가지 이상을 동시에 사용한다.
- 감자 대왕과 네 확장 대왕은 HP 3이며 유효 타격마다 다음 단계로 간다.
- 훌라후프 대왕은 회전 중 밟기를 막고 훌라후프 발사가 끝나 멈춘 동안만 약점이 열린다.
- 투명 대왕은 매 phase 도달 가능한 다섯 위치 중 하나를 보여준 뒤 숨는다. 공개 위치를 기억해 밟아야 하며 오답은 공격으로 되돌아온다.
- 물 대왕의 보스용 웅덩이는 일반 수역과 분리한다. 보스전 동안 숨 감소를 정지하고, 출현 파문·기포·소리로 위치를 함께 예고한다.
- 랜덤 대왕은 비전투 결과가 최대 3회 연속 나온 뒤 반드시 전투를 시작한다. 재도전 진입은 일반 1초·쉬움 1.5초 안전 시간을 보장한다.
- 약점 시간에는 머리 위 별빛을 보여주며, 처치 즉시 공격과 충돌을 중지하고 무지개 게이트를 만든다.
- 게이트는 빛나는 바깥 테두리, 번갈아 나타나는 별 반짝임, 잠시 떠올랐다 내려오는 부유 동작으로 도착 지점을 강조한다.

## 카메라와 피드백

- 이동 방향 앞으로 120~180px의 Look-Ahead를 둔다.
- 위치 보간과 세로 Dead Zone을 사용한다.
- 위험·목적지를 미리 보여주는 레벨 카메라 힌트를 데이터로 둔다.
- 변신 100~180ms 강조, 보스 처치 200ms 정지 프레임, 약한 화면 흔들림을 사용한다.
- 화면 흔들림은 기본적으로 약하게 하고 설정에서 끌 수 있다.
- Look-Ahead는 속도 방향을 따라 좌우로 전환한다. 역방향 스테이지에서도 탈출 방향을 보여주고, 오른쪽 파도 경고가 왼쪽 시야를 과도하게 줄이지 않게 한다.

## 접근성

- 화면 흔들림 On/Off.
- BGM/효과음 볼륨과 전체 음소거.
- 키보드/게임패드 조작 안내.
- 쉬운 모드: HP +2, 추락 점수 손실 없음, 비행 소모 감소, 보스 예고 증가, 추가 체크포인트.
- 색 정보만으로 상태를 전달하지 않고 실루엣·아이콘·애니메이션을 함께 사용한다.
- `화면 효과 강도`를 `보통 / 약하게` 중에서 선택해 안개 농도·수중 왜곡·파도 화면 효과를 함께 조절한다.
- 음소거 상태에서도 숨 부족과 파도 접근 방향·시간을 시각만으로 판독할 수 있어야 한다.
- 쉬운 모드의 파도·숨·레이저·투사체 완화는 구간을 건너뛰게 하지 않고 예고와 대응 시간을 늘리는 방향으로만 적용한다.

## 데이터 구조 원칙

- Phaser npm 패키지를 사용한다.
- `GameScene`은 레벨 ID만 알고, 적·아이템·지형 좌표를 포함하지 않는다.
- 보스는 별도 Scene 없이 레벨의 `type: "boss"` section으로 동작한다.
- 배치는 레벨 데이터, 적 행동은 `data/enemies.js`, 지형은 Tiled JSON에 둔다.
- `LevelLoader`, `ObjectiveManager`, `ProgressManager`가 데이터를 해석한다.
- 이미지·오디오 로딩 실패 시 이름이 보이는 도형과 무음 fallback으로 끝까지 진행한다.
- 새로운 레벨은 데이터 파일 1개와 레지스트리 2줄만 추가한다.
- 신규 레벨은 Schema version 2의 진행 방향·명시적 출구·환경 데이터를 사용한다. 기존 version 1 레벨은 정규화 기본값으로 계속 동작한다.
- 발판 단위 장치는 `TerrainMechanicsManager`, 쓰나미·물·안개·레이저 같은 구간 전체 장치는 `EnvironmentMechanicsManager`가 맡는다.
- 숨 상태와 UI 이벤트는 `BreathManager`, 익사 피해는 `HealthManager`의 환경 피해 진입점을 사용한다. `GameScene`에는 특정 스테이지 이름을 하드코딩하지 않는다.

## 범위 밖

- 온라인 기능, 계정, 과금, 경쟁 랭킹.
- 원안에 없는 캐릭터 능력 차이.
- 2P 동시 플레이(향후 확장 지점만 문서화).
- 참조 상용 캐릭터·구도·UI의 모사.
- 마법 보호막·마법 벽, 적을 모으는 바람·단일 적 토네이도는 확장 P0 범위에서 보류한다.
